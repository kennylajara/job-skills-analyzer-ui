version: '3'

services:
  app:
    image: kennylajara/job-skills-analyzer-ui
    build:
      context: .
      dockerfile: Dockerfile
      target: prod

networks:
  default:
    name: nginxproxymanager_default
    external: true
